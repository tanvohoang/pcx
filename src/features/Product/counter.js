import { useState } from "react";

export default function counter() {
    const [count,setCount] = useState[0]
    const increment = () => count += count;
    const decrement = () => count -= count;
    const incrementByAmount = () => count += action.payload;
}